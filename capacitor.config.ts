import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'Multiple_Choice_App',
  webDir: 'dist',
  bundledWebRuntime: false
};

export default config;
